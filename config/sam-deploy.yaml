version: 0.2
        
phases:
  build:
    on-failure: ABORT
    commands:
      - echo Deploying SAM App
      - sam deploy --stack-name aws-sam-jenkins-pipeline-tutorial
      
artifacts:
  files:
    - '**/*'
  name: sam_deploy_output
  